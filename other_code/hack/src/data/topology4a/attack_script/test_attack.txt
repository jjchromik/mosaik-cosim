for i in 0 to 1000
    for server in get listservers
        connect server
        for branch in get listbranches
            set v get sensordata of getstate branch False
            setsensor branch, v*1.01
            wait 0.5
        forEnd
    forEnd
forEnd